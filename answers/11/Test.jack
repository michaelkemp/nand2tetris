class Test {
    field int a, b, c;
    static char d, e, f;
    field boolean yes, no;
    static int s1, d1, l1;

    constructor Test new(int xa, int xb, int xc) {
        var boolean test1, testa, thingmy;
        var char test2;
        var Test test3;
        let a = xa[5+a] * 5;
        let b = (xb + 4) * 9 +  myMeth1("w", false, 3 * 4 + -6);
        let c = 11/xc;
        return this;
    }

    method int myMeth1(char a, boolean b, int i) {
        var int exit;
        let exit = i + 5 / (7*3);
        return exit;
    }

    method int double(int a) {
        var int thingy;
        let thingy = myMeth1("l", false, 9 * -3 * 4 + -6 * Test.triple(7 * 6));

        return -a * 2 * myMeth1("a", true, thingy) + 11 / (4 - 5);

    }

    function int triple(int a) {
        return a * 3;
    }



}